name = "solana-guard"
version = "0.1.0"
description = "Policy-bound Solana transaction firewall: verify decoded bytes against user intent and operator limits"
author = "OutstandingVick"
wasm_path = "solana_guard.wasm"
capabilities = ["tool"]
# Jailed, plugin-local policy configuration only. No network, files, wallet,
# signing, or broadcasting capabilities.
permissions = ["config_read"]
