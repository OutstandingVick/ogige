name = "ogige"
version = "0.1.0"
description = "Solana transaction safety gate: decode, narrate, and ALLOW/HOLD/REJECT before the agent signs"
author = "ogige"
wasm_path = "ogige.wasm"
capabilities = ["tool"]
# No host permissions yet — pure offline decode + risk classification.
# Optional http_client can be added later for RPC enrichment / simulation.
permissions = []
