[sop]
name = "solana-transaction-review"
description = "Re-check an unsigned Solana transaction and park it at a durable human approval checkpoint"
version = "0.2.0"
execution_mode = "supervised"
max_concurrent = 1
admission_policy = "hold"
max_pending_approvals = 8

[[triggers]]
type = "manual"
